<script type="text/javascript" charset="utf-8">
  $(function() {
  new Highcharts.Chart({
    chart: {renderTo: "URLs"},
    title: {text: "Speed Score"},
    xAxis: {type: "datetime"},
    yAxis: {title: {text: "(n/100)"}},
    series: [{
      name: 'URL A',
      pointInterval: <%= 1.day * 1000 %>,
      pointStart: <%= 3.weeks.ago.to_i * 1000 %>,
      data: [20, 30, 40, 50, 90]
    },  {
      name: 'URL B',
      pointInterval: <%= 1.day * 1000 %>,
      pointStart: <%= 3.weeks.ago.to_i * 1000 %>,
      data: [05, 15, 30, 40, 100]
    },  {
      name: 'URL C',
      pointInterval: <%= 1.day * 1000 %>,
      pointStart: <%= 3.weeks.ago.to_i * 1000 %>,
      data: [50, 36, 24, 12, 89]
    }],
  });
}); </script> 

<div id="URLs" style="width:1000px; height:450px;"></div>

<div class="page-header">
  <%= link_to new_site_path, class: 'btn btn-primary' do %>
    <span class="glyphicon glyphicon-plus"></span>
    New Site
  <% end %>
  <h1>Your Sites</h1>
</div>

<div class="table-responsive">
  <table class="table table-striped table-bordered table-hover">
    <thead>
    <tr>
      <th>URL</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
    </thead>

    <tbody>
    <%= content_tag_for(:tr, @sites) do |site| %>
      <td><%= site.url %></td>
      <td><%= link_to 'Show', site %></td>
      <td><%= link_to 'Edit', edit_site_path(site) %></td>
      <td><%= link_to 'Destroy', site, method: :delete, data: {confirm: 'Are you sure?'} %></td>
    <% end %>
    </tbody>
  </table>
</div>
